#ifndef LANG_VAR
#define LANG_VAR
#include <string> 
#include <iostream>
#include <fstream>
#include <vector>
#include "utils.hpp"
using namespace std;

void variable_init(vector<Token> tokens,string var_scope,string scope,string directory);

#endif